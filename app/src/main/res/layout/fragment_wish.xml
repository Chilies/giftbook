<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@color/themeRed"
        android:orientation="horizontal">
        <Switch
            android:id="@+id/fragment_wish_switch_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:switchMinWidth="20dp"
            android:layout_marginStart="10dp"
            android:layout_marginLeft="10dp"
            android:layout_gravity="center"
            android:textColor="@color/firstTitle"
            android:textOn="@string/activity_main_switch_on"
            android:textOff="@string/activity_main_switch_off"
            android:thumb="@drawable/thumb"
            android:track="@drawable/track"
            />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/fragment_wish_title"
            android:layout_weight="1"
            android:textColor="@color/white"
            android:textSize="20sp"
            />
        <ImageView
            android:id="@+id/fragment_wish_add_img"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="10dp"
            android:layout_marginLeft="10dp"
            android:layout_gravity="center"
            android:src="@drawable/add"/>
    </LinearLayout>

    <ListView
        android:id="@+id/fragment_wish_listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:dividerHeight="3dp"
        android:fadingEdge="none"
        android:scrollbars="none"
        />

</LinearLayout>